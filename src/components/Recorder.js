import React, { Component } from 'react';

class Recorder extends Component {

    render() {
        return (
            <button>
                録音
            </button>
        );
    }
}

export default Recorder;
