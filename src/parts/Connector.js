import React, { Component } from 'react';

class Connector extends Component {
    render() {
        return (
            <p>
                <textarea></textarea>
                <button>接続</button>
            </p>
        );
    }
}

export default Connector;
